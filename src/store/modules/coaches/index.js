const state = {
    coaches: []
}

const mutations = {
    'SET_COACHES'(state, payload) {
        state.coaches = payload
    }
}

const actions = {

}

const getters = {
    
}

export default { state, mutations, actions, getters }